jQuery(function ($) {
    $('.secret-source').secretSource({
        includeTag: false
    });
    $('#banner-fade').bjqs({
        height: 150,
        width: 1000,
        responsive: true
    });
});